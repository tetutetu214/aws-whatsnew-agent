# 監査とロードマップ（2026-07-06 / Fable 5 作成）

Fable 5 キャンペーン終了前の「計画の作り置き」。以降のセッションはこの優先順で実行する。状況が変わっていたら premise-check してから従うこと。

## 現状スナップショット

- Phase1 は本番稼働中。LINE 到達確認（07-04）・定期実行観測（07-05, 07-06 エラーなし・重複送信防止も正常）まで**完了済み**。メモリの「次は LINE 到達+定期実行観測」は古い。
- 唯一の進行中タスク: **Nova Micro の要約品質実測**（サンプル1件で敬体/常体混在を確認済み、数日分貯めてから判定する方針）。
- PR #1〜#3 すべてマージ済み、main clean、テスト 19/19 pass。

## 監査所見（リスク順）

1. **[中] アラート機構が存在しない**: CloudWatch Alarm / SNS が一切なく、障害検知は Logs の目視依存。観測フェーズが終わって誰も見なくなった瞬間に、Lambda 失敗や LINE 送信失敗（line.py:96-103 は握りつぶして継続する設計）が静かに埋もれる。**毎朝届くはずの通知が「届かないこと」に気づく仕組みがない**のが本質的な穴。
2. **[低] spec.md と実装の乖離**: spec.md:57 の IAM 記述に `dynamodb:BatchGetItem` が残っているが、実装は GetItem/PutItem のみ（PR#1 で削除済み）。ドキュメント更新漏れ。
3. **[低] マージ済みブランチ2本の残存**: feature/phase1-implementation, fix/ssm-reserved-param-name。掃除のみ。
4. **[低] EXCLUDE_SERVICES フィルタが未実測**: 実装済みだが空文字運用。使う時が来たら動作確認から。

## ロードマップ（この順で実行）

### Step 1: 要約品質の判定（数日分の実データが貯まったら）
- CloudWatch Logs ではなく DynamoDB に保存済みの summary を数日分並べて、敬体/常体混在・情報落ちを評価
- NG なら stacks/whatsnew_stack.py:33 のモデルIDを nova-lite 等へ変更して再デプロイ（1行変更）。判定結果は knowledge.md へ

### Step 2: 死活アラートの追加（小PR、Phase2 の前に必ず）
- 推奨: EventBridge Scheduler 実行後の Lambda エラーを CloudWatch Alarm → SNS → 既存 LINE 経路（または email）で通知
- 判断根拠: 「通知が来ない」障害は利用者（てつてつ本人）が数日気づけない。Phase2 でエージェント化して複雑になる前に、最小の観測装置を入れておくのが安い
- 実装は Codex / implementer 委譲可（CDK 数十行）。デプロイはてつてつの aws login 後

### Step 3: docs 整合と掃除（chore、main 直接）
- spec.md:57 の IAM 記述修正、マージ済みブランチ削除

### Step 4: Phase2（エージェント化）の要件洗い出し
- 着手前に「Phase1 のままで困っていることは何か」を1行で言語化してから（エージェント化が目的化しないように）。AgentCore 採用可否はその要件次第で判断

## 担当分業の目安

- Step 1 の品質判定: てつてつ+Claude で実データを見る
- Step 2 実装: Codex または implementer。デプロイ検証: Claude（aws login 後）
- Step 3: サブエージェントで十分
