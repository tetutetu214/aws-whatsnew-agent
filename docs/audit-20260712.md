# 監査 2026-07-12（Fable・audit-20260706 の更新版）

Phase2（図解生成エージェント）本番デプロイ完成後の初監査。主題は死活監視の構造穴の設計。

## 現状

- main ブランチ。07-08 以降は Phase2 実装・出典リンク追加・README 構成図刷新・S3 ライフサイクル失効廃止が完了（PR #7 マージ済み）。
- 前回監査の所見1「アラート機構なし」は PR #4 で**部分消化**: `stacks/whatsnew_stack.py:294-321` に `WorkerErrorAlarm`（5分/1件エラー）と `WorkerMissingInvocationAlarm`（24時間デッドマンスイッチ）があり、`AlertTopic`（:279-283、Email 購読は ALERT_EMAIL 依存）へ接続済み。EventBridge Scheduler 停止もデッドマン経由で間接検知できる。

## 所見1（中）: Phase2 コンポーネントが全て死活監視の対象外

監視があるのは Worker Lambda 1つだけ。Phase2 で増えた以下は**失敗しても誰にも通知されない**。

| コンポーネント | 現状 | 設計判断 |
|---|---|---|
| ExplainerDispatcher Lambda（:225-241） | アラームなし | `metric_errors` アラームを AlertTopic に接続（ボタン押下時のみ動く低頻度なのでデッドマン不要） |
| SettingsWebhook Lambda（:62-86） | アラームなし | `metric_errors` アラーム追加。ただし**例外時も 200 を返す設計**（LINE 再送での二重適用防止）のため、握りつぶされた失敗はエラーメトリクスに乗らない。ログメトリックフィルタでの補完は「実害が出たら」（コールドスタート対策と同じ判断基準）でよい |
| ExplainerViewer Lambda（:200-214） | アラームなし | 本人閲覧のみの低リスクだが、実装3行で足せるので一括で `metric_errors` 付与 |
| AgentCore Runtime（図解生成本体） | 監視なし | CDK 側から直接メトリクスを張るより、**dispatcher が invoke 結果を検証して失敗時に例外を投げる**設計にし、dispatcher のエラーアラームに集約するのが最小構成。前提確認: dispatcher が現在 invoke 失敗を握りつぶしていないか（未確認、着手時に最初に見る） |

実装規模: CDK 追記 30〜40 行 ＋ dispatcher の例外伝播確認。50 行未満のため Codex 委譲基準未満、次セッションで直接実装してよい（テストは cdk synth ＋既存 pytest 回帰）。

## 所見2（低・前回所見の後始末）

- 前回所見4（EXCLUDE_SERVICES 未実測）は 07-08 に EXCLUDE_SERVICES 自体が廃止され**失効**。クローズ。
- 前回所見2（spec.md の IAM 記述に `dynamodb:BatchGetItem` 残存）・所見3（マージ済みブランチ残存）は今回未検証。次回 docs 掃除のついでに確認。

## 次の一手（優先順）

1. ⏳てつてつ: LINE 図解ボタンの実機タップ確認・SNS 購読確認メールの Confirm（AlertTopic の Email 購読が Confirm 待ちのままだと**アラームが飛んでも届かない**。所見1の前提でもある）
2. 死活アラート実装（所見1の表どおり: 3 Lambda に metric_errors ＋ dispatcher の例外伝播確認）
3. Nova Micro 要約の品質判定（数日分のデータ蓄積待ち）
